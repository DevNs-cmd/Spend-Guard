# packages/shared-types

TypeScript types/DTOs shared between `apps/web` and `apps/api`, so the two
never drift out of sync. Edited via PR by whoever is adding the field —
tag the module owner it belongs to as a reviewer.
