# modules/budgets — Owner: Gauri

See `/docs/team/GAURI_README.md`. Owns Budget + Alert entities and the
alert-dispatch/notifier logic. Reads current spend from `usage` module
(Vedant) — does not compute cost itself.
