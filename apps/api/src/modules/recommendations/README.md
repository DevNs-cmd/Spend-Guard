# modules/recommendations — Owner: Krrish

See `/docs/team/KRRISH_README.md`. Reads usage/cost data (Vedant) and
provider/model metadata (this module's own providers submodule) to produce
suggestions — does not modify budgets/alerts (Gauri) or usage data directly.
