# modules/usage — Owner: Vedant

See `/docs/team/VEDANT_README.md`. Owns the core data models (UsageRecord,
CostRecord, Project) and the cost-calculation engine. This is the module
everyone else reads from — treat schema changes here as high-impact and
announce them before merging.
