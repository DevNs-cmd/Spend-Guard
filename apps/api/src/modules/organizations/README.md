# modules/organizations — Owner: Neerav

See `/docs/team/NEERAV_README.md`. Owns Organization, Membership/Role, and
audit logs. Every other module depends on `Organization` + `Membership`
entities exported from here — do not fork/duplicate them elsewhere.
