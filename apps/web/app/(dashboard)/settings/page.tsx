// Org settings: tags, users/teams, notification prefs, retention.
// Data source: organizations (Neerav) + tags (Vedant).
export default function SettingsPage() {
  return <div>Admin & Settings</div>;
}
