# apps/web — Frontend (Owner: Anuj)

Entire Next.js frontend. Full task detail: `/docs/team/ANUJ_README.md`.

**Do not edit `apps/api/**` from here.** Consume it only through
`lib/api-client.ts` and the shared types in `packages/shared-types`.
If an endpoint you need doesn't exist yet, add it to `docs/API_CONTRACTS.md`
and flag it to the owning backend teammate — don't build around it with mocks
that stay in the final code.
